import React from 'react';
import { Empty } from 'antd';
import './Empty.scss';

const NoData = () => {
    return (
        <div className="Empty">
            <Empty />
        </div>
    );
};

export default NoData;
